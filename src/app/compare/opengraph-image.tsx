// The hub reuses the site's default OG image. Comparison pages under
// /compare/[slug] define their own pair-specific image, which overrides this.
export { default, alt, size, contentType } from "../opengraph-image";
